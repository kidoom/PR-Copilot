{
  "name": "pr-copilot",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"python -m uvicorn backend.main:app --host localhost --port 8001 --reload --reload-dir backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "python -m uvicorn backend.main:app --host localhost --port 8001 --reload --reload-dir backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
