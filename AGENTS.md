# PR-Copilot 工作流规则

## Git 提交规则

- **main 分支**：永远不要直接 push。所有改动必须通过 PR 提交，等团队 review 后合并。
- **Liziark 分支**：可以直接 push 到远程，不需要走 PR。
- **流程**：先提交到 Liziark（直接 push），再创建 PR 到 main（等 review）。

## PR 提交规范

创建 PR 时，必须在描述中包含：

- **做了什么**：这次改动的核心内容
- **实现了什么**：新增的功能、模块、能力
- **修复了什么**：解决的 bug、问题、安全风险
- **测试了什么**：测试覆盖情况、测试结果（如 `N/N tests pass`）
- **验证方式**：编译检查、lint 检查等
